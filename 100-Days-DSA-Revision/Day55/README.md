# Day 55 - Doubly Linked List

## 📌 Overview
Day 55 of my 100 Days DSA Revision Series focused on implementing a Doubly Linked List using Python.

## 📚 Topics Covered
- Doubly Linked List
- Node structure
- `next` and `prev` pointers
- Insertion at beginning
- Insertion at end
- Insertion at a specific position
- Deletion at beginning
- Deletion at end
- Deletion at a specific position
- Traversing and displaying the list
- Handling empty lists and invalid positions

## 🧠 Key Learning
A Doubly Linked List stores two references in every node:
- `next` → points to the next node
- `prev` → points to the previous node

While inserting or deleting nodes, both links must be updated correctly.

## 💻 Implementation
The following operations were implemented:

- `insert_at_beginning()`
- `insert_at_end()`
- `insert_at_position()`
- `delete_at_beginning()`
- `delete_at_end()`
- `delete_at_position()`
- `display()`

## ⏱️ Time Complexity
- Insert at beginning: O(1)
- Insert at end: O(n)
- Insert at position: O(n)
- Delete at beginning: O(1)
- Delete at end: O(n)
- Delete at position: O(n)
- Display: O(n)

## 📂 File
`doubly_linked_list.py`

## 🎯 Goal
Strengthen understanding of linked-list pointer manipulation and implement insertion and deletion operations in a Doubly Linked List.

## 🚀 Progress
Day 55/100 completed.

